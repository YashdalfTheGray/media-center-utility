server = require('./../../server/server.js');

describe('Express app server', function() {
	it('pending server tests', function() {
		pending('testing express routes and features in the future');
	});
});